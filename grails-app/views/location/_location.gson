import org.etive.city4age.repository.Location

model {
	Location location
}

json {
	id location.id
	locationId location.locationId
	name location.name
	type location.type
	latitude location.latitude
	longitude location.longitude
}
