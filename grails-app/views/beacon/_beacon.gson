import org.etive.city4age.repository.Beacon

model {
	Beacon beacon
}

json {
    id beacon.id
	beaconId beacon.beaconId
	locationId beacon.location.locationId
	location beacon.location.name + ": " + beacon.description
}
